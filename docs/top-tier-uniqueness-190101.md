# Top-Tier Uniqueness (`190101`)

## 结论

在 [toyota_seg_IGN_ON_20260312_190101_000.ndjson](../logs/toyota_seg_IGN_ON_20260312_190101_000.ndjson) 中，`phase_sum >= 120` 的顶级事件只有一簇，没有第二个独立 cluster。

## 实际命中

全段仅有这 4 个顶级点：

| ts | phase | phase_sum | family131 | family260 |
| --- | --- | ---: | --- | --- |
| `1773342250785` | `43 48` | 139 | `fff4` | `fff4` |
| `1773342250810` | `44 46` | 138 | `fff4` | `fff4` |
| `1773342250831` | `42 40` | 130 | `fff4` | `fff4` |
| `1773342250856` | `3e 3c` | 122 | `fff4` | `fff0` |

## 这代表什么

### 1. top-tier template 是稀有模式

它不是整段 driving log 中反复出现的常态，而是非常稀少的高值 cluster。

### 2. 先前滑动窗口找到的多次命中，其实都属于同一簇

不是有很多个独立 top-tier 事件，而是：

- 同一个高位平台
- 被不同窗口重复覆盖到

### 3. 这提高了它的筛选价值

因为如果某个模式在长段 log 里只有一簇，它就更适合当：

- 高优先级被动候选

而不是一般 driving 状态的常见型态。

## 当前最稳的说法

`190101` 中真正的 top-tier candidate 目前只有一簇：

- 进入前有中高 phase ramp
- 平台上落在 `fff4|fff4`
- 峰值达到 `43 48 / 44 46 / 42 40`
- 退出时退到 `fff4|fff0`

## 对后续的意义

之后如果在新 log 里再次看到同型 cluster，就会非常有价值，因为：

- 目前样本数还只有一簇
- 每多一个复现样本，可信度都会显著提升
