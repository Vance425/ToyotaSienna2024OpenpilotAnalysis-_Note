# 示範分析: toyota_seg_IGN_ON_20260311_184921_000

## 分析對象

- Log: [toyota_seg_IGN_ON_20260311_184921_000.ndjson](/D:/Codex/toyota-sienna-tsk-analysis/analysis-input/raw_can_logs/toyota_seg_IGN_ON_20260311_184921_000.ndjson)
- bus: `0`
- 目的: 示範第一輪人工篩選怎麼做

## 本輪只做什麼

這一輪不試圖解出 `TSK`，只做三件事：

1. 確認 6 個重點 ID 在這段裡是否活躍
2. 看哪些 ID 比較像 reference
3. 看哪些 ID 比較像 candidate

## 6 個重點 ID 活躍度

- `0x260`: `7877` frames
- `0x115`: `6667` frames
- `0x116`: `6690` frames
- `0x131`: `6539` frames
- `0x610`: `349` frames
- `0xD5`: `7745` frames

## 第一眼角色判讀

### 0xD5

- count 很高
- `uniq_data` 只有 `9`
- 前 80 筆平均週期約 `26.861 ms`

判讀：

- 很像穩定的 speed/state reference
- 這和前面 `v23` 把 `0xD5` 當 speed reference 一致

### 0x610

- count 明顯低很多
- `uniq_data` `172`
- 前 80 筆平均週期約 `550.684 ms`

判讀：

- 很像低頻的 steering reference / 狀態對照線
- 不像高頻 auth/counter frame

### 0x116

- `uniq_data = 6690`，幾乎每筆都不同
- `uniq_prefix4 = 231`
- `uniq_suffix4 = 6690`

判讀：

- 前半段相對穩定，後半段幾乎每筆都變
- 這非常符合「core 比較穩、tail 很像 auth/counter」的候選結構
- 目前看起來是這一輪最乾淨的 SecOC-like 候選

### 0x115

- `uniq_data = 6667`
- `uniq_prefix4 = 797`
- `uniq_suffix4 = 6667`

判讀：

- 也很像 tail 高變化的 auth/counter 結構
- 比 `0x116` 稍微沒有那麼乾淨，因為前半段變化比 `0x116` 多
- 但仍然是高價值候選

### 0x131

- `uniq_data = 6539`
- `uniq_prefix4 = 4235`
- `uniq_suffix4 = 6539`

判讀：

- 前半段就已經變化很多
- 比較像 control/state 與 auth-like 混在一起的訊號
- 很值得留在候選群，但不像 `0x116` 那麼像純 tail-protected 結構

### 0x260

- `uniq_data = 7496`
- `uniq_prefix4 = 1069`
- `uniq_suffix4 = 7496`
- 前 80 筆平均週期約 `24.177 ms`

判讀：

- 高頻
- 後半段幾乎完全動態
- 前半段也不是固定不動
- 很像 control/state/protected family 的核心 frame

## 原始樣本的直觀觀察

從開頭樣本就能看到：

- `0xD5` 多次出現 `08000000000000e5`，非常穩
- `0x116` 常常長得像 `00000000????????`
- `0x115` 常常長得像 `00000002????????`
- `0x131` 常常長得像 `00??ffc3????????`
- `0x260` 常常長得像 `00ff..ffc3......`

這代表：

- `0x115 / 0x116 / 0x131 / 0x260` 都不是亂選的
- 它們確實呈現出「部分位元組像 core、部分位元組像 tail」的味道

## 這一輪最重要的示範結論

### 可以先放心的事

你現在不是完全無從下手，因為這段 log 裡已經能看出角色差異：

- `0xD5` 像參考線
- `0x610` 像參考線
- `0x116 / 0x115` 像 tail-protected 候選
- `0x131` 像 state + protected 混合候選
- `0x260` 像核心控制 / 狀態候選

### 目前最值得先盯的排序

1. `0x116`
2. `0x115`
3. `0x260`
4. `0x131`
5. `0x610` 作 steering reference
6. `0xD5` 作 speed reference

## 下一輪怎麼做

這一輪只是「辨角色」。

下一輪才是：

- 對齊 `0x610 / 0xD5`
- 找 `0x260 / 0x116 / 0x115 / 0x131` 在特定時間窗口的共同變化
- 特別看 `comma 3X` 失敗前後是否有 tail reset、停頓或模式切換
