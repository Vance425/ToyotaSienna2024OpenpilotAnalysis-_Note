# Driving `n0` Validation (2026-03-12)

## 目標

驗證 `0x116.tail.n0` 的 `+4` backbone 在 `2026-03-12` 的 driving log 裡是否仍然成立。

樣本：

- $label (local-only source path)
- [toyota_seg_IGN_ON_20260312_190101_000.ndjson](../logs/toyota_seg_IGN_ON_20260312_190101_000.ndjson)

## 樣本 A: `18:58:01`

### 主要 phase

- `17 16`: `6 -> a` (`+4`)
- `16 16`: `e -> 6` (`+8`) -> `b` (`+5`) -> `f` (`+4`) -> `3` (`+4`) -> `7` (`+4`) -> `b` (`+4`) -> `f` (`+4`)

### 判讀

這段很有代表性：

- phase 進入穩定段後，`+4` 很快成為主骨架
- 但在 phase 剛切好時，會先出現 `+8`、`+5` 這種重定相步進

也就是：

- driving 樣本裡 `n0` 仍然可建模
- 只是重定相成本比靜態樣本更高

## 樣本 B: `19:01:01`

### 主要 phase

- `1c 1c`: `3 -> 7` (`+4`) -> `4` (`+13`) -> `8` (`+4`)
- `1b 1b`: `0 -> 4` (`+4`)
- `1a 1a`: `c -> 0` (`+4`)
- `19 19`: `8 -> 0` (`+8`)

### 判讀

這段比 `18:58:01` 更動態：

- phase 更短
- 單一 phase 內可用樣本更少
- 但 `+4` 仍然反覆出現

唯一明顯的離群點是：

- `1c 1c` 中的 `7 -> 4` (`+13`)

這很像 driving 條件下的額外修正步進，而不是模型失效。

## 合併結論

`n0` 的 `+4` backbone 在 driving log 裡仍然成立，但需要加一個條件：

- phase 越短、邊界越密，重定相插入步進就越多

換句話說：

- 靜態樣本：`+4` 更乾淨
- driving 樣本：`+4` 仍是主骨架，但更常被 `+8` / `+5` / `+13` 打斷

## 更新後的工作模型

現在對 `n0` 的描述可以更準確：

- `n0` 是 phase-aware rolling nibble
- 主骨架為 `+4`
- 在 phase 邊界或高動態 driving 條件下，會插入修正步進

## 對整個任務的意義

這個結果很重要，因為它說明：

- `n0` 不是只在靜態窗口才成立的假象
- 在真實 driving log 裡仍然存在
- 只是要把 boundary / steady 分段一起納入模型

## 下一步

如果還要繼續深挖，現在最值得做的是：

1. 針對 `n0` 建立「允許步進集合」：`+4` 為主，`+8/+5/+13` 為修正項
2. 再檢查這些修正項是否總發生在 phase 邊界附近
